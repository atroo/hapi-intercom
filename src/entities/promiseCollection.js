"use strict";

module.exports = function(promises, providerNames) {
   
    var promiseCollection = function() {};
    
    var pub = promiseCollection.prototype;
    
    pub.getPromiseByProvider = function(provider) {
        
    };
    pub.getPromises = function(providers) {
        
    };
    
    return new promiseCollection();
};